# 📐 Android Build & Proxy Architecture

Visual guide to understanding how everything works together.

---

## 🏗️ Build Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    DIVIDELA ANDROID BUILD FLOW                  │
└─────────────────────────────────────────────────────────────────┘

                           ┌──────────────┐
                           │ Developer    │
                           │  (You)       │
                           └──────┬───────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
              [Quick Test]              [Build APK]
                    │                           │
                    ▼                           ▼
          ┌──────────────────┐      ┌──────────────────┐
          │  npx expo start  │      │ ./build-android  │
          │                  │      │      .sh         │
          └────────┬─────────┘      └────────┬─────────┘
                   │                         │
                   ▼                         ▼
          ┌──────────────────┐      ┌──────────────────┐
          │   Expo Go App    │      │   EAS Build      │
          │   (on device)    │      │   (cloud)        │
          └────────┬─────────┘      └────────┬─────────┘
                   │                         │
                   │                         ▼
                   │                ┌──────────────────┐
                   │                │  Build Process   │
                   │                │  (~15-20 min)    │
                   │                └────────┬─────────┘
                   │                         │
                   │                         ▼
                   │                ┌──────────────────┐
                   │                │   APK File       │
                   │                │   (download)     │
                   │                └────────┬─────────┘
                   │                         │
                   └─────────┬───────────────┘
                             │
                             ▼
                   ┌──────────────────┐
                   │  Android Device  │
                   │  (Install & Run) │
                   └──────────────────┘
```

---

## 🔄 NPM Proxy Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    NPM PROXY CONFIGURATION                      │
└─────────────────────────────────────────────────────────────────┘

     ┌──────────────┐
     │ npm install  │
     └──────┬───────┘
            │
            ▼
     ┌──────────────────┐
     │ Read .npmrc      │
     │ (project local)  │
     └──────┬───────────┘
            │
     ┌──────┴───────────────────┐
     │                          │
     ▼                          ▼
┌─────────────┐          ┌─────────────┐
│ Proxy: ON   │          │ Proxy: OFF  │
└──────┬──────┘          └──────┬──────┘
       │                        │
       ▼                        ▼
┌──────────────────┐    ┌──────────────────┐
│ localhost:8081   │    │ Direct Internet  │
└────────┬─────────┘    └────────┬─────────┘
         │                       │
         └───────┬───────────────┘
                 │
                 ▼
         ┌──────────────────┐
         │ npm Registry     │
         │ (download pkgs)  │
         └────────┬─────────┘
                  │
                  ▼
         ┌──────────────────┐
         │ node_modules/    │
         │ (installed)      │
         └──────────────────┘
```

---

## 📦 File Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                     PROJECT FILE STRUCTURE                      │
└─────────────────────────────────────────────────────────────────┘

dividela/
│
├── 🔧 CONFIG FILES (auto-used)
│   ├── .npmrc ────────────┐
│   │   └─> Used by: npm install
│   │
│   ├── eas.json ──────────┐
│   │   └─> Used by: eas build
│   │
│   ├── .env ──────────────┐
│   │   └─> Used by: Firebase config
│   │
│   └── package.json ──────┐
│       └─> Used by: npm, expo, eas
│
├── 🛠️ HELPER SCRIPTS (you run)
│   ├── build-android.sh ──┐
│   │   └─> Runs: eas build (with menu)
│   │
│   └── toggle-proxy.sh ───┐
│       └─> Modifies: .npmrc
│
├── 📚 DOCUMENTATION (you read)
│   ├── INDEX.md (you are here)
│   ├── QUICK_START_ANDROID.md
│   ├── BUILD_ANDROID.md
│   ├── BUILD_CHECKLIST.md
│   ├── NPM_PROXY_CONFIG.md
│   ├── PROXY_TROUBLESHOOTING.md
│   └── SETUP_SUMMARY.md
│
└── 💻 SOURCE CODE
    ├── App.js
    ├── src/
    │   ├── screens/
    │   ├── components/
    │   └── config/
    └── assets/
```

---

## 🔐 Build Profiles Comparison

```
┌─────────────────────────────────────────────────────────────────┐
│                      EAS BUILD PROFILES                         │
└─────────────────────────────────────────────────────────────────┘

┌────────────────┬────────────────┬────────────────┬──────────────┐
│   FEATURE      │  DEVELOPMENT   │    PREVIEW     │  PRODUCTION  │
├────────────────┼────────────────┼────────────────┼──────────────┤
│ Output         │ APK            │ APK            │ APK or AAB   │
│ Size           │ Large (~60MB)  │ Medium (~40MB) │ Small (~20MB)│
│ Dev Tools      │ ✅ Included    │ ❌ Removed     │ ❌ Removed   │
│ Optimization   │ ❌ None        │ ⚡ Basic       │ ⚡⚡ Full     │
│ Minification   │ ❌ No          │ ✅ Yes         │ ✅ Yes       │
│ Use Case       │ Internal Test  │ Beta Testing   │ Play Store   │
│ Build Time     │ ~15-20 min     │ ~15-20 min     │ ~20-25 min   │
├────────────────┼────────────────┼────────────────┼──────────────┤
│ COMMAND        │ --profile dev  │ --profile prev │ --profile pr │
└────────────────┴────────────────┴────────────────┴──────────────┘

RECOMMENDED FOR TESTING: Preview Profile ⭐
```

---

## 🌐 Proxy vs No Proxy Decision Tree

```
┌─────────────────────────────────────────────────────────────────┐
│              SHOULD YOU USE THE PROXY?                          │
└─────────────────────────────────────────────────────────────────┘

                    npm install
                         │
                         ▼
              ┌──────────────────┐
              │ Is proxy server  │
              │   running?       │
              └────┬─────────┬───┘
                   │         │
                YES│         │NO
                   ▼         ▼
         ┌──────────────┐   ┌──────────────┐
         │ Use Proxy    │   │ Disable      │
         │ (.npmrc ON)  │   │ (.npmrc OFF) │
         └──────┬───────┘   └──────┬───────┘
                │                  │
                ▼                  ▼
         ┌──────────────┐   ┌──────────────┐
         │ npm install  │   │ toggle-proxy │
         │              │   │    .sh off   │
         └──────┬───────┘   └──────┬───────┘
                │                  │
                │                  ▼
                │           ┌──────────────┐
                │           │ npm install  │
                │           └──────┬───────┘
                │                  │
                └────────┬─────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ Dependencies │
                  │  Installed ✅ │
                  └──────────────┘
```

---

## 🎯 Build Type Selection Guide

```
┌─────────────────────────────────────────────────────────────────┐
│            WHICH BUILD SHOULD YOU USE?                          │
└─────────────────────────────────────────────────────────────────┘

    What do you want to do?
            │
    ┌───────┴───────────────────┬─────────────────┐
    │                           │                 │
    ▼                           ▼                 ▼
┌─────────┐              ┌──────────┐      ┌───────────┐
│ Quick   │              │ Test on  │      │ Release   │
│ Test    │              │ Real     │      │ to Play   │
│ Changes │              │ Device   │      │ Store     │
└────┬────┘              └────┬─────┘      └─────┬─────┘
     │                        │                   │
     ▼                        ▼                   ▼
┌──────────┐          ┌──────────────┐    ┌──────────────┐
│ Expo Go  │          │ EAS Preview  │    │ EAS Producti│
│ (fastest)│          │ (recommended)│    │     on       │
└────┬─────┘          └──────┬───────┘    └──────┬───────┘
     │                       │                    │
     │                       │                    │
COMMAND:              COMMAND:            COMMAND:
npx expo start        eas build -p        eas build -p
                      android --profile   android --profile
                      preview             production
```

---

## 🔄 Development Workflow

```
┌─────────────────────────────────────────────────────────────────┐
│               TYPICAL DEVELOPMENT WORKFLOW                      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ 1. SETUP    │  npm install (one time)
└──────┬──────┘  eas login (one time)
       │
       ▼
┌─────────────┐
│ 2. DEVELOP  │  Write code in src/
└──────┬──────┘  Edit screens, components
       │
       ▼
┌─────────────┐
│ 3. TEST     │  npx expo start
└──────┬──────┘  Test in Expo Go
       │
       ▼
┌─────────────┐  Code working?
│ 4. VERIFY   │────NO───┐
└──────┬──────┘         │
       │               │
      YES              │
       │               │
       ▼               │
┌─────────────┐        │
│ 5. BUILD    │        │
│    APK      │        │
└──────┬──────┘        │
       │               │
       ▼               │
┌─────────────┐        │
│ 6. TEST ON  │        │
│   DEVICE    │        │
└──────┬──────┘        │
       │               │
       ▼               │
┌─────────────┐        │
│ 7. ISSUES?  │────YES─┘
└──────┬──────┘   (go back to develop)
       │
      NO
       │
       ▼
┌─────────────┐
│ 8. RELEASE  │  Production build
└─────────────┘  Submit to Play Store
```

---

## 🛠️ Tools & Their Purpose

```
┌─────────────────────────────────────────────────────────────────┐
│                    TOOL RELATIONSHIPS                           │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   npm        │──> Installs dependencies
│              │──> Reads .npmrc for proxy
└──────────────┘

┌──────────────┐
│   expo       │──> Development server
│   (CLI)      │──> Quick testing with Expo Go
└──────────────┘

┌──────────────┐
│   eas        │──> Cloud build service
│   (CLI)      │──> Creates APK/AAB files
│              │──> Reads eas.json for config
└──────────────┘

┌──────────────┐
│  Firebase    │──> Backend services
│              │──> Reads .env for config
└──────────────┘

┌──────────────┐
│  Proxy       │──> Routes npm traffic
│  (optional)  │──> Configured in .npmrc
└──────────────┘
```

---

## 📊 File Size Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    BUILD SIZE COMPARISON                        │
└─────────────────────────────────────────────────────────────────┘

Source Code (MB)
    │
    ├─> Development Build
    │       │
    │       ├─> Include Dev Tools        ✅
    │       ├─> Include Source Maps      ✅
    │       ├─> Minification             ❌
    │       └─> Size: ~60 MB
    │
    ├─> Preview Build
    │       │
    │       ├─> Include Dev Tools        ❌
    │       ├─> Include Source Maps      ⚠️  (minimal)
    │       ├─> Minification             ✅
    │       └─> Size: ~40 MB  ⭐ (recommended)
    │
    └─> Production Build
            │
            ├─> Include Dev Tools        ❌
            ├─> Include Source Maps      ❌
            ├─> Minification             ✅
            ├─> Tree Shaking             ✅
            ├─> Optimization             ✅
            └─> Size: ~20 MB  (smallest)
```

---

## 🎓 Complexity Levels

```
┌─────────────────────────────────────────────────────────────────┐
│            BUILD METHODS BY COMPLEXITY                          │
└─────────────────────────────────────────────────────────────────┘

BEGINNER        INTERMEDIATE       ADVANCED
   │                  │                │
   ▼                  ▼                ▼
┌────────┐      ┌─────────┐     ┌──────────┐
│ Expo   │      │  EAS    │     │  Local   │
│  Go    │      │ Build   │     │  Build   │
└───┬────┘      └────┬────┘     └────┬─────┘
    │                │               │
Instant        15-20 min        Requires
No account     Free account     Android Studio
Limited        Full features    Full control
features       Good for prod    Complex setup
```

---

## 🎯 Success Path

```
┌─────────────────────────────────────────────────────────────────┐
│              PATH TO SUCCESSFUL BUILD                           │
└─────────────────────────────────────────────────────────────────┘

START
  │
  ├─> 1. Read QUICK_START_ANDROID.md (5 min)
  │
  ├─> 2. Check Prerequisites
  │      ├─> Node.js installed? ✅
  │      ├─> Expo account? ✅
  │      └─> Firebase config? ✅
  │
  ├─> 3. Configure Proxy (if needed)
  │      └─> Run: ./toggle-proxy.sh status
  │
  ├─> 4. Install Dependencies
  │      └─> Run: npm install
  │
  ├─> 5. Build APK
  │      └─> Run: ./build-android.sh
  │             └─> Choose: Preview (option 1)
  │
  ├─> 6. Wait (~15-20 min) ☕
  │
  ├─> 7. Download APK
  │      └─> Click link in terminal
  │
  ├─> 8. Install on Device
  │      ├─> Enable "Unknown Sources"
  │      └─> Install APK
  │
  └─> 9. Test App
         └─> Launch and verify all features
            │
            ▼
        SUCCESS! 🎉
```

---

## 📞 Quick Reference

```
┌─────────────────────────────────────────────────────────────────┐
│                    COMMAND QUICK MAP                            │
└─────────────────────────────────────────────────────────────────┘

WANT TO...                    │  RUN THIS
──────────────────────────────┼─────────────────────────────────
Build APK                     │  ./build-android.sh
Test quickly                  │  npx expo start
Check proxy status            │  ./toggle-proxy.sh status
Disable proxy                 │  ./toggle-proxy.sh off
Enable proxy                  │  ./toggle-proxy.sh on
Install dependencies          │  npm install
Check build status            │  eas build:list
Login to Expo                 │  eas login
Clear cache                   │  npx expo start -c
View build logs               │  (click build URL)
──────────────────────────────┴─────────────────────────────────
```

---

**Next Steps:**
1. Read `QUICK_START_ANDROID.md` for actual commands
2. Use `BUILD_CHECKLIST.md` when building
3. Reference this file to understand how everything connects

**Visual learner?** This diagram shows you the big picture! 🎨
